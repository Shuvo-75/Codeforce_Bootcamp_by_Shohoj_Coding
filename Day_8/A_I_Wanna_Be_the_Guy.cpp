// Problem link: https://codeforces.com/problemset/problem/469/A

#include <bits/stdc++.h>

using namespace std;

int main()
{
    int t;
    cin >> t;

    int p; 
    cin >> p; 
    vector<int> arr(p); 
    for(int i = 0; i < p; i++)
    {
        cin >> arr[i];
    }

    int q; 
    cin >> q; 
    vector<int> new_arr(q);
    for(int i = 0; i < q; i++)
    {
        cin >> new_arr[i];
    }

    
}